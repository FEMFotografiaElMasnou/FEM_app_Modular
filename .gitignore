# Configuración local de Claude Code (no es del proyecto)
.claude/

# Dependencias (por si en el futuro se usa npm)
node_modules/

# Archivos del sistema operativo
Thumbs.db
Desktop.ini
.DS_Store

# Logs y temporales
*.log
*.tmp

# Notas de Enric (solo apuntes, no van al repo)
[Ee]nric*.md

# Arranque local de desarrollo (herramienta personal, no se despliega)
server.js
Iniciar_FEM.bat
